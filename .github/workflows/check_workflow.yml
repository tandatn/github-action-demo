name: Check Workflow

on:
  workflow_dispatch:
    inputs:
      chosen-os:
        required: true
        type: choice
        options:
          - ubuntu-latest
          - windows-2022

jobs:
  check-something:
    runs-on: ${{ inputs.chosen-os }}
    steps:
      - run: echo Hello, World!
